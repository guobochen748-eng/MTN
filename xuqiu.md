# CISC7202 Experiment Report-3（3D Generation）总结与分步完成指南
## 一、实验核心总结
本次实验聚焦**基于扩散模型的前沿3D内容生成技术**，核心目标是从文本提示词生成3D物体，同时探究3D生成的常见失效模式、超参数对生成效果和训练稳定性的影响，以及模型的显存占用与计算效率问题。实验需完成指定任务并撰写至少2页的ACM格式LaTeX报告，**提交截止时间为3月15日23:59**，提交平台为UM Moodle，报告需包含实验介绍、详细过程与观察、失效分析、改进建议及超参数敏感性反思等核心内容。

### 实验关键信息
1. **核心代码库**：https://github.com/Texaser/MTN
2. **参考资料**：实验视频演示（https://www.youtube.com/watch?v=LH6-wKg30FQ）、PyTorch版本兼容说明（https://pytorch.org/get-started/previous-versions）
3. **环境工具**：需安装显存监控工具`gpustat`，若出现CUDA显存不足，需将分辨率从64降至48或切换轻量版Stable Diffusion
4. **重要注意事项**：训练完成后勿随意删除/重命名检查点文件；若需重训，需先备份检查点文件避免覆盖

## 二、实验分步完成指南
### 步骤1：实验环境搭建与准备
1. **确认PyTorch兼容性**：访问PyTorch官方兼容页面，检查本地Python、CUDA版本与MTN代码库要求的PyTorch版本匹配，若不匹配需安装对应版本。
2. **克隆代码库**：在终端执行命令`git clone https://github.com/Texaser/MTN.git`，将代码拉取到本地，进入项目根目录。
3. **安装依赖库**：根据项目`requirements.txt`安装所需依赖，执行`pip install -r requirements.txt`（若文件无明确要求，按需安装PyTorch、torchvision、diffusers等扩散模型相关库）。
4. **安装显存监控工具**：执行`pip install gpustat`，验证安装成功：在终端输入`gpustat`，能显示GPU型号、显存占用、利用率即完成。
5. **显存不足预处理**：若本地GPU显存较小，提前将代码中生成分辨率从64调整为48，或替换为轻量版Stable Diffusion模型，避免训练中出现CUDA out of memory错误。

### 步骤2：完成核心实验任务
本次实验需完成4个核心任务，需按要求记录所有操作、命令和实验结果，为报告撰写准备素材。
#### 任务1：文本提示词生成3D视频
1. 选择指定提示词（如`a tiger dressed as a doctor`），也可自定义符合要求的3D物体文本提示词；
2. 在MTN项目根目录，执行代码中3D生成的核心命令（参考项目`README.md`或`main.py`说明），**准确记录执行的完整命令**；
3. 等待训练/生成完成，保存生成的3D视频文件、日志文件，记录生成耗时、是否出现中断/报错等情况。

#### 任务2：全程监控GPU显存使用
1. 打开新的终端窗口，在3D生成命令执行**前**，输入`gpustat`记录初始显存状态；
2. 生成过程中，按固定时间间隔（如每1分钟）执行`gpustat`，**持续记录GPU显存占用率、已用显存、剩余显存**，可将结果整理为表格（含时间、显存数据）；
3. 生成完成后，再次执行`gpustat`记录最终显存状态，分析训练不同阶段（初始化、模型训练、生成推理）的显存变化规律。

#### 任务3：识别并分析至少2种3D生成失效案例
1. 查看生成的3D视频/物体，从**几何畸变、部件缺失、纹理伪影、形状失真、视角不一致**等常见问题中，筛选至少2种失效情况；
2. 对每种失效案例进行详细描述，**建议截取对应失效位置的截图**（报告中需附视觉示例），记录失效出现的训练阶段/生成环节。

#### 任务4：超参数调整与效果观察
1. 打开MTN项目的`main.py`，定位到22-173行的`config/hyper-parameters`区域，查看所有可调整的超参数（如学习率、视角数量、生成分辨率、训练轮数、批次大小等）；
2. **仅修改其中1个超参数**（避免多变量干扰结果），例如将学习率从默认值调大/调小，或调整视角数量；
3. 保持其他参数不变，重新执行3D生成命令，记录新的生成结果、耗时、显存占用、失效情况；
4. 对比**原参数**与**调整后参数**的实验结果，分析该超参数对3D生成质量、训练稳定性、计算效率的具体影响。

### 步骤3：实验数据与素材整理
完成所有任务后，统一整理以下实验素材，为LaTeX报告撰写做准备：
1. 命令记录：3D生成的原始命令、超参数调整后的命令；
2. 显存数据：`gpustat`记录的所有显存占用表格，含时间、GPU状态；
3. 视觉素材：3D生成结果截图、失效案例截图、超参数调整前后对比截图；
4. 结果分析：失效案例的原因初步分析、超参数的影响规律总结；
5. 改进思路：针对观察到的问题，梳理至少2个具体的3D生成改进建议。

### 步骤4：撰写LaTeX实验报告
报告需使用课程提供的ACM格式LaTeX模板（下载地址：https://www.overleaf.com/read/qvygwgddscth#ca1a3a），**最低2页**，严格按要求包含以下模块：
1. **简要介绍与目标总结**：说明实验的核心目的、3D生成的技术背景、本次实验的主要任务；
2. **实验详细描述与观察**：依次介绍环境搭建、4个核心任务的执行过程、记录的实验数据（命令、显存变化、生成结果）、超参数调整的具体操作；
3. **缺陷/失效分析**：结合截图，详细分析至少2种失效案例的表现、出现原因，说明失效对3D生成效果的影响；
4. **改进建议**：提出至少2个**具体、可落地**的改进方案，如更好的模型初始化方法、多阶段优化细化、针对性的数据增强策略等；
5. **超参数敏感性反思**：总结所调整超参数的敏感性，分析其对生成质量、训练稳定性、显存/计算效率的影响，提出该参数的最优调整方向。

#### LaTeX模板使用要点
1. 进入Overleaf模板地址，点击左上角`home`，复制模板为可编辑项目；
2. 删除模板中无关内容，替换为自己的实验报告内容；
3. 按课程要求引用参考文献（若有），通过Google Scholar获取BibTeX格式引用，粘贴到项目的bib文件中，在`main.tex`中调用；
4. 插入实验截图：将图片转为PDF/PNG格式，按LaTeX语法插入，标注图注（如“图1：3D生成的老虎医生模型失效案例——几何畸变”）；
5. 完成后重新编译项目，生成PDF格式报告，检查格式、页数、内容是否符合要求。

### 步骤5：报告提交与文件备份
1. **提交截止**：在3月15日23:59前，登录UM Moodle，找到Experiment Report-3提交入口，上传编译好的PDF报告；
2. **文件备份**：将实验代码、生成的3D视频、显存记录、LaTeX项目文件、PDF报告统一备份，避免文件丢失；
3. **检查点备份**：若后续有重训需求，执行命令`mv trial/checkpoints/df_ep0060.pth df_ep0060_backup.pth`备份检查点文件，防止意外覆盖。

### 步骤6：实验收尾与问题排查
若实验中出现报错（如显存不足、代码运行失败、生成结果异常），按以下方式排查：
1. 显存不足：继续降低分辨率、减少批次大小、关闭其他占用GPU的程序；
2. 代码报错：检查PyTorch版本、依赖库是否安装完整，参考MTN项目的Issues区寻找解决方案；
3. 生成效果差：检查超参数设置、提示词设计，尝试更换轻量版模型或调整扩散模型的采样步数；
4. 无法解决：及时联系课程TA或授课老师获取帮助。